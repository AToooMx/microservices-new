server:
  port: 0

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: DB
  application:
    name: product-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
